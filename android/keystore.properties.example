# 复制为 android/keystore.properties(已 gitignore),填入真实值。
# 用现有 TWA keystore —— 不能换,否则老用户无法覆盖升级。
# alias 用 `keytool -list -keystore ~/anxinmao-twa/android.keystore` 查到的那个。
storeFile=/绝对路径/android.keystore
storePassword=你的store口令
keyAlias=你的alias
keyPassword=你的key口令
