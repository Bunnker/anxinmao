# 大模型 API —— 分诊与行为问答的服务端调用。密钥仅服务端使用,不进浏览器。
# DeepSeek 与 通义(DashScope)都走 OpenAI 兼容接口 —— 填了哪个,服务端就用哪个。
# 两个都填时默认用 DeepSeek;要强制指定可设 LLM_PROVIDER=deepseek 或 qwen。
# 用法:把本文件复制成 .env.local,填入你自己的 key。
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=
# LLM_PROVIDER=deepseek
# DEEPSEEK_MODEL=deepseek-chat
# QWEN_MODEL=qwen-plus
